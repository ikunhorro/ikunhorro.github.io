<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>ManimCE 安装/使用教程</title>
    <url>/ManimCE/</url>
    <content><![CDATA[<h1 id="manimCE-安装教程"><a href="#manimCE-安装教程" class="headerlink" title="manimCE 安装教程"></a>manimCE 安装教程</h1><p><a href="https://www.manim.community/">link</a></p>
<h2 id="安装-Python"><a href="#安装-Python" class="headerlink" title="安装 Python"></a>安装 Python</h2><p><a href="https://www.python.org/downloads/">link</a></p>
<p>往下找到需要的版本点开，然后拉到最后，即可下载 Python 安装包。</p>
<p>（这里以 Python 3.11.8 作为示例，其它版本可能会有 bug）</p>
<p>下载后打开安装程序，把最下方的 <code>Add python.exe to PATH</code> 的勾点亮，然后点击上方的 <code>Install Now</code>，然后等待安装完毕即可。</p>
<h2 id="安装-Scoop"><a href="#安装-Scoop" class="headerlink" title="安装 Scoop"></a>安装 Scoop</h2><p><a href="https://scoop.sh/">link</a></p>
<p>按 Win+X 然后点击终端，输入主页的这两串代码：<br><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser</span><br></pre></td></tr></table></figure><br><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Invoke-RestMethod -Uri https://get.scoop.sh | Invoke-Expression</span><br></pre></td></tr></table></figure></p>
<h2 id="安装-ffmpeg"><a href="#安装-ffmpeg" class="headerlink" title="安装 ffmpeg"></a>安装 ffmpeg</h2><p>打开终端，然后输入 manimCE 主页的这段代码：<br><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">scoop install python ffmpeg</span><br></pre></td></tr></table></figure></p>
<p>scoop 会先安装 7-Zip，然后下载 ffmpeg 并解压。</p>
<p>等待安装完成即可。</p>
<h2 id="安装-manim"><a href="#安装-manim" class="headerlink" title="安装 manim"></a>安装 manim</h2><p>打开终端，然后输入 manimCE 主页的这段代码：<br><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">python -m pip install manim</span><br></pre></td></tr></table></figure></p>
<p>等待安装完成即可。</p>
<h2 id="安装-LaTeX"><a href="#安装-LaTeX" class="headerlink" title="安装 LaTeX"></a>安装 LaTeX</h2><p>打开终端，然后输入 manimCE 主页的这段代码：<br><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">scoop install latex</span><br></pre></td></tr></table></figure></p>
<p>过一会会出现 MikTex 的安装程序，等待安装完成即可。</p>
<h1 id="manimCE-使用教程"><a href="#manimCE-使用教程" class="headerlink" title="manimCE 使用教程"></a>manimCE 使用教程</h1><p>打开终端，输入 <code>manim</code>，如果出现版本号，那就说明你安装成功了。</p>
<p>在任意一个目录下新建一个 Python 文件命名为 <code>6.py</code>，在里面输入如下的代码：<br><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> manim <span class="keyword">import</span> *</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">LaTeXTemplateLibrary</span>(<span class="title class_ inherited__">Scene</span>):</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">construct</span>(<span class="params">self</span>):</span><br><span class="line">        tex = Tex(<span class="string">&#x27;Hello 你好 \\LaTeX&#x27;</span>, tex_template = TexTemplateLibrary.ctex, font_size = <span class="number">144</span>)</span><br><span class="line">        self.add(tex)</span><br></pre></td></tr></table></figure><br>然后打开终端，输入 <code>cd [python 文件的目录]</code>（不需要中括号），然后输入 <code>manim -pql .\6.py</code>，即可运行，这段代码会自动帮你安装中文转 svg 的程序，这样就可以用 <code>Tex</code> 函数来显示中文。</p>
<p>接下来输入这段代码：<br><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> manim <span class="keyword">import</span> *</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">SineCurveUnitCircle</span>(<span class="title class_ inherited__">Scene</span>):</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">construct</span>(<span class="params">self</span>):</span><br><span class="line">        self.show_axis()</span><br><span class="line">        self.show_circle()</span><br><span class="line">        self.move_dot_and_draw_curve()</span><br><span class="line">        self.wait()</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">show_axis</span>(<span class="params">self</span>):</span><br><span class="line">        x_start = np.array([-<span class="number">6</span>,<span class="number">0</span>,<span class="number">0</span>])</span><br><span class="line">        x_end = np.array([<span class="number">6</span>,<span class="number">0</span>,<span class="number">0</span>])</span><br><span class="line"></span><br><span class="line">        y_start = np.array([-<span class="number">4</span>,-<span class="number">2</span>,<span class="number">0</span>])</span><br><span class="line">        y_end = np.array([-<span class="number">4</span>,<span class="number">2</span>,<span class="number">0</span>])</span><br><span class="line"></span><br><span class="line">        x_axis = Line(x_start, x_end)</span><br><span class="line">        y_axis = Line(y_start, y_end)</span><br><span class="line"></span><br><span class="line">        self.add(x_axis, y_axis)</span><br><span class="line">        self.add_x_labels()</span><br><span class="line"></span><br><span class="line">        self.origin_point = np.array([-<span class="number">4</span>,<span class="number">0</span>,<span class="number">0</span>])</span><br><span class="line">        self.curve_start = np.array([-<span class="number">3</span>,<span class="number">0</span>,<span class="number">0</span>])</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">add_x_labels</span>(<span class="params">self</span>):</span><br><span class="line">        x_labels = [</span><br><span class="line">            MathTex(<span class="string">&quot;\pi&quot;</span>), MathTex(<span class="string">&quot;2 \pi&quot;</span>),</span><br><span class="line">            MathTex(<span class="string">&quot;3 \pi&quot;</span>), MathTex(<span class="string">&quot;4 \pi&quot;</span>),</span><br><span class="line">        ]</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="built_in">len</span>(x_labels)):</span><br><span class="line">            x_labels[i].next_to(np.array([-<span class="number">1</span> + <span class="number">2</span>*i, <span class="number">0</span>, <span class="number">0</span>]), DOWN)</span><br><span class="line">            self.add(x_labels[i])</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">show_circle</span>(<span class="params">self</span>):</span><br><span class="line">        circle = Circle(radius=<span class="number">1</span>)</span><br><span class="line">        circle.move_to(self.origin_point)</span><br><span class="line">        self.add(circle)</span><br><span class="line">        self.circle = circle</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">move_dot_and_draw_curve</span>(<span class="params">self</span>):</span><br><span class="line">        orbit = self.circle</span><br><span class="line">        origin_point = self.origin_point</span><br><span class="line"></span><br><span class="line">        dot = Dot(radius=<span class="number">0.08</span>, color=YELLOW)</span><br><span class="line">        dot.move_to(orbit.point_from_proportion(<span class="number">0</span>))</span><br><span class="line">        self.t_offset = <span class="number">0</span></span><br><span class="line">        rate = <span class="number">0.25</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">def</span> <span class="title function_">go_around_circle</span>(<span class="params">mob, dt</span>):</span><br><span class="line">            self.t_offset += (dt * rate)</span><br><span class="line">            mob.move_to(orbit.point_from_proportion(self.t_offset % <span class="number">1</span>))</span><br><span class="line"></span><br><span class="line">        <span class="keyword">def</span> <span class="title function_">get_line_to_circle</span>():</span><br><span class="line">            <span class="keyword">return</span> Line(origin_point, dot.get_center(), color=BLUE)</span><br><span class="line"></span><br><span class="line">        <span class="keyword">def</span> <span class="title function_">get_line_to_curve</span>():</span><br><span class="line">            x = self.curve_start[<span class="number">0</span>] + self.t_offset * <span class="number">4</span></span><br><span class="line">            y = dot.get_center()[<span class="number">1</span>]</span><br><span class="line">            <span class="keyword">return</span> Line(dot.get_center(), np.array([x,y,<span class="number">0</span>]), color=YELLOW_A, stroke_width=<span class="number">2</span> )</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">        self.curve = VGroup()</span><br><span class="line">        self.curve.add(Line(self.curve_start,self.curve_start))</span><br><span class="line">        <span class="keyword">def</span> <span class="title function_">get_curve</span>():</span><br><span class="line">            last_line = self.curve[-<span class="number">1</span>]</span><br><span class="line">            x = self.curve_start[<span class="number">0</span>] + self.t_offset * <span class="number">4</span></span><br><span class="line">            y = dot.get_center()[<span class="number">1</span>]</span><br><span class="line">            new_line = Line(last_line.get_end(),np.array([x,y,<span class="number">0</span>]), color=YELLOW_D)</span><br><span class="line">            self.curve.add(new_line)</span><br><span class="line"></span><br><span class="line">            <span class="keyword">return</span> self.curve</span><br><span class="line"></span><br><span class="line">        dot.add_updater(go_around_circle)</span><br><span class="line"></span><br><span class="line">        origin_to_circle_line = always_redraw(get_line_to_circle)</span><br><span class="line">        dot_to_curve_line = always_redraw(get_line_to_curve)</span><br><span class="line">        sine_curve_line = always_redraw(get_curve)</span><br><span class="line"></span><br><span class="line">        self.add(dot)</span><br><span class="line">        self.add(orbit, origin_to_circle_line, dot_to_curve_line, sine_curve_line)</span><br><span class="line">        self.wait(<span class="number">8.5</span>)</span><br><span class="line"></span><br><span class="line">        dot.remove_updater(go_around_circle)</span><br></pre></td></tr></table></figure></p>
<p>这是一个关于 $\sin$ 函数的程序，运行它，我们可以用以下的指令：</p>
<ul>
<li><p><code>manim -pql .\6.py</code> 480p 15帧</p>
</li>
<li><p><code>manim -pqm .\6.py</code> 720p 30帧</p>
</li>
<li><p><code>manim -pqh .\6.py</code> 1080p 60帧</p>
</li>
<li><p><code>manim -pqp .\6.py</code> 1440p 60帧</p>
</li>
<li><p><code>manim -pqk .\6.py</code> 2160p 60帧</p>
</li>
</ul>
<p>其中，<code>p</code> 代表预览，<code>q</code> 代表生产，<code>l</code>/<code>m</code>/<code>h</code>/<code>p</code>/<code>k</code> 为视频的分辨率和帧数。</p>
]]></content>
      <categories>
        <category>教程</category>
        <category>manim</category>
      </categories>
      <tags>
        <tag>教程</tag>
        <tag>manim</tag>
      </tags>
  </entry>
  <entry>
    <title>hexo + github 搭建个人博客</title>
    <url>/blog/</url>
    <content><![CDATA[<h1 id="hexo-github-搭建个人博客"><a href="#hexo-github-搭建个人博客" class="headerlink" title="hexo + github 搭建个人博客"></a>hexo + github 搭建个人博客</h1><p><strong>以下代码全都在 <code>powershell</code> 里执行</strong></p>
<p><strong><code>username</code> 都代表你 <code>github</code> 用户名</strong></p>
<h2 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h2><p>首先来看 <a href="https://hexo.io/zh-cn/"><code>hexo</code></a>。</p>
<p><code>hexo</code> 要由 <code>node.js</code> 的软件包管理器 <code>npm</code> 安装。此外 <code>hexo</code> 依赖于 <code>git</code>，所以要现安装 <code>node.js</code> 和 <code>git</code>。</p>
<ul>
<li><p><a href="https://git-scm.com/downloads">git</a></p>
</li>
<li><p><a href="https://nodejs.org/en/">nodejs</a></p>
</li>
</ul>
<p>然后使用 <code>npm</code> 安装 <code>hexo</code>：</p>
<figure class="highlight ps"><table><tr><td class="code"><pre><span class="line">npm install hexo<span class="literal">-cli</span> <span class="literal">-g</span></span><br></pre></td></tr></table></figure>
<p>关于 windows 系统上禁止运行脚本：</p>
<p>在设置里查找 <code>powershell</code>，找到并打开 <code>允许本地 powershell 脚本在未签名的情况下运行</code>。</p>
<h2 id="建站"><a href="#建站" class="headerlink" title="建站"></a>建站</h2><p>首先到要建立博客的路径（这里以 <code>D:\</code> 为例）：</p>
<figure class="highlight ps"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> D:\</span><br></pre></td></tr></table></figure>
<p>然后运行以下代码：</p>
<figure class="highlight ps"><table><tr><td class="code"><pre><span class="line">hexo init Blog; <span class="built_in">cd</span> Blog; npm install</span><br></pre></td></tr></table></figure>
<p>然后 <code>Blog</code> 文件夹里会变成这样：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Blog</span><br><span class="line">├── _config.yml</span><br><span class="line">├── package.json</span><br><span class="line">├── scaffolds</span><br><span class="line">├── source</span><br><span class="line">|   ├── _drafts</span><br><span class="line">|   ├── _posts</span><br><span class="line">└── themes</span><br></pre></td></tr></table></figure>
<p><code>_config.yml</code> 是配置网站的地方，<code>source</code> 里面存储的都是网站的文章，其中 <code>_posts</code> 的内容会被编译成 <code>html</code>，<code>_drafts</code> 目录会被忽略，而其他文件会原样复制到网站的根目录。<code>themes</code> 文件夹里存储了主题文件。</p>
<p>这时，<code>_posts</code> 文件夹里会默认有一篇 <code>helloworld</code> 文章。</p>
<h2 id="运行"><a href="#运行" class="headerlink" title="运行"></a>运行</h2><p>先运行以下代码：</p>
<figure class="highlight ps"><table><tr><td class="code"><pre><span class="line">hexo clean; hexo g; hexo s</span><br></pre></td></tr></table></figure>
<p><code>hexo clean</code> 是删除网页，<code>hexo g</code> 是生成网页（在 <code>public</code> 文件夹下），<code>hexo s</code> 是生成服务器，和你在同一个局域网的人就可以访问到。</p>
<p>生成默认在 <code>4000</code> 端口，如果被占用，我们还可以选择其它端口（这里以 <code>1145</code> 为例）：</p>
<figure class="highlight ps"><table><tr><td class="code"><pre><span class="line">hexo s <span class="literal">-p</span> <span class="number">1145</span></span><br></pre></td></tr></table></figure>
<p>运行结束之后，会输出一个网址，把它打开，就是你的博客了。</p>
<h2 id="配置"><a href="#配置" class="headerlink" title="配置"></a>配置</h2><p><code>_config.yml</code> 文件包括了站点的基础属性。</p>
<p>其中的大部分内容我们不用改。我们只要改基本信息：</p>
<figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Site</span></span><br><span class="line"><span class="attr">title:</span> <span class="string">Hexo</span></span><br><span class="line"><span class="attr">subtitle:</span> <span class="string">&#x27;&#x27;</span></span><br><span class="line"><span class="attr">description:</span> <span class="string">&#x27;&#x27;</span></span><br><span class="line"><span class="attr">keywords:</span></span><br><span class="line"><span class="attr">author:</span> <span class="string">John</span> <span class="string">Doe</span></span><br><span class="line"><span class="attr">language:</span> <span class="string">en</span></span><br><span class="line"><span class="attr">timezone:</span> <span class="string">&#x27;&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># URL</span></span><br><span class="line"><span class="comment">## Set your site url here. For example, if you use GitHub Page, set url as &#x27;https://username.github.io/project&#x27;</span></span><br><span class="line"><span class="attr">url:</span> <span class="string">http://example.com</span></span><br><span class="line"><span class="attr">permalink:</span> <span class="string">:year/:month/:day/:title/</span></span><br><span class="line"><span class="attr">permalink_defaults:</span></span><br><span class="line"><span class="attr">pretty_urls:</span></span><br><span class="line">  <span class="attr">trailing_index:</span> <span class="literal">true</span> <span class="comment"># Set to false to remove trailing &#x27;index.html&#x27; from permalinks</span></span><br><span class="line">  <span class="attr">trailing_html:</span> <span class="literal">true</span> <span class="comment"># Set to false to remove trailing &#x27;.html&#x27; from permalinks</span></span><br></pre></td></tr></table></figure>
<p>这里是网站的基本信息。<code>title</code> 是主标题，<code>subtitle</code> 是副标题。<code>description</code> 可以是你的一句座右铭，<code>keywords</code> 越多越好，用逗号隔开。<code>author</code> 写你的名字，<code>language</code> 选 <code>zh-CN</code>，<code>timezone</code> 选 <code>Asia/Shanghai</code>。<code>url</code> 就填写你的网址（<code>username.github.io</code>）。</p>
<h2 id="github"><a href="#github" class="headerlink" title="github"></a>github</h2><p>在 <a href="https://github.com/"><code>github</code></a> 上注册一个账号，然后登录。</p>
<p>登陆后点击右上角的 <code>+</code>，然后点击 <code>New repository</code>，把仓库名字命名为 <code>username.github.io</code>，然后建立仓库。</p>
<p>上传 <code>public</code> 需要配置 <code>ssh-key</code>：</p>
<figure class="highlight ps"><table><tr><td class="code"><pre><span class="line">ssh<span class="literal">-keygen</span> <span class="literal">-t</span> rsa <span class="literal">-b</span> <span class="number">4096</span></span><br><span class="line"><span class="comment"># 一路回车，直到出现如下字样：</span></span><br><span class="line"><span class="comment"># Your identification has been saved in ~/.ssh/id_rsa</span></span><br><span class="line"><span class="comment"># Your public key has been saved in ~/.ssh/id_rsa.pub</span></span><br><span class="line"><span class="comment"># The key fingerprint is:</span></span><br><span class="line"><span class="comment"># SHA256: ###############################</span></span><br><span class="line"><span class="comment"># The key&#x27;s randomart image is:</span></span><br><span class="line"><span class="comment"># +---[RSA ####]----+</span></span><br><span class="line"><span class="comment"># |##########       |</span></span><br><span class="line"><span class="comment"># |##########       |</span></span><br><span class="line"><span class="comment"># |##########       |</span></span><br><span class="line"><span class="comment"># |##########       |</span></span><br><span class="line"><span class="comment"># |##########       |</span></span><br><span class="line"><span class="comment"># |##########       |</span></span><br><span class="line"><span class="comment"># |##########       |</span></span><br><span class="line"><span class="comment"># |##########       |</span></span><br><span class="line"><span class="comment"># |##########       |</span></span><br><span class="line"><span class="comment"># +----[SHA256]-----+</span></span><br><span class="line"><span class="comment"># #号部分是你的SHA256信息。</span></span><br></pre></td></tr></table></figure>
<p>然后运行：</p>
<figure class="highlight ps"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cat</span> ~/.ssh/id_rsa.pub</span><br></pre></td></tr></table></figure>
<p>然后在 <code>github</code> 中点击自己的头像，找到 <code>Setting</code> 点击，在左边找到 <code>SSH and GPG keys</code>，然后点 <code>New SSH Key</code>。名字随便取，内容把 <code>id_rsa.pub</code> 里的内容复制进去就可以了。</p>
<h2 id="插件"><a href="#插件" class="headerlink" title="插件"></a>插件</h2><h3 id="上传"><a href="#上传" class="headerlink" title="上传"></a>上传</h3><p><code>hexo</code> 有一个插件 <code>deploy</code>，可以将已经生成好的 <code>public</code> 文件夹自动上传，不过需要我们配置一下。</p>
<p>安装插件 <code>hexo-deployer-git</code>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">npm install hexo-deployer-git --save</span><br></pre></td></tr></table></figure>
<p>然后打开之前提到的 <code>_config.yml</code>，找到 <code>deploy</code> 字段，更改如下：</p>
<figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Deployment</span></span><br><span class="line"><span class="comment">## Docs: https://hexo.io/docs/one-command-deployment</span></span><br><span class="line"><span class="attr">deploy:</span></span><br><span class="line">  <span class="attr">type:</span> <span class="string">git</span></span><br><span class="line">  <span class="attr">repo:</span> <span class="string">git@github.com:username/userame.github.io</span></span><br><span class="line">  <span class="attr">branch:</span> <span class="string">main</span></span><br></pre></td></tr></table></figure>
<p>现在只要一行命令，就可以直接编译并部署了：</p>
<figure class="highlight ps"><table><tr><td class="code"><pre><span class="line">hexo clean; hexo g; hexo d</span><br></pre></td></tr></table></figure>
<h3 id="分类和标签"><a href="#分类和标签" class="headerlink" title="分类和标签"></a>分类和标签</h3><p>在 hexo 中，分类和标签不是一个东西。他们的形式很像：</p>
<figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="attr">categories:</span> <span class="comment">#分类</span></span><br><span class="line"><span class="bullet">-</span> <span class="string">category1</span></span><br><span class="line"><span class="bullet">-</span> <span class="string">category2</span></span><br><span class="line"><span class="comment">#...</span></span><br><span class="line"><span class="attr">tags:</span> <span class="comment">#标签</span></span><br><span class="line"><span class="bullet">-</span> <span class="string">tag1</span></span><br><span class="line"><span class="bullet">-</span> <span class="string">tag2</span></span><br><span class="line"><span class="comment">#...</span></span><br></pre></td></tr></table></figure>
<p>但是他们的含义不一样。<code>categories</code> 表示文章属于 <code>category1</code> 类别中的 <code>category2</code> 类别，而 <code>tags</code> 表示文章既是 <code>tag1</code> 标签，又是 <code>tag2</code> 标签。可以看到 <code>categories</code> 是有严格父子关系的，而 <code>tags</code> 是并列的。</p>
<p>如果希望读者能够通过分类和标签索引文章，需要新建 <code>categories</code> 页面和 <code>tags</code> 页面：</p>
<figure class="highlight ps"><table><tr><td class="code"><pre><span class="line">hexo new page categories; hexo new page tags</span><br></pre></td></tr></table></figure>
<p>此时在 <code>source</code> 文件夹下就会多出 <code>categories</code> 文件夹和 <code>tags</code> 文件夹。文件夹中有一个 <code>index.md</code>。打开，在 <code>front-matter</code> 中分别加入 <code>type: &quot;categories&quot;</code> 和 <code>type: &quot;tags&quot;</code>。正文留空。就完成了构建。</p>
<h3 id="关于"><a href="#关于" class="headerlink" title="关于"></a>关于</h3><p>同理，新建 <code>about</code> 页面。</p>
<figure class="highlight ps"><table><tr><td class="code"><pre><span class="line">hexo new page about</span><br></pre></td></tr></table></figure>
<p>编辑 <code>source/about/index.md</code>，加入 <code>type: &quot;about&quot;</code>，然后在正文部分编辑自我介绍内容。</p>
<h3 id="文章排序"><a href="#文章排序" class="headerlink" title="文章排序"></a>文章排序</h3><p><code>hexo</code> 默认的排序是根据发布日期。有时我们会希望将文章置顶或按照 <code>update</code> 日期排序，可以利用一个插件实现：</p>
<figure class="highlight ps"><table><tr><td class="code"><pre><span class="line">npm uninstall hexo<span class="literal">-generator-index</span> <span class="literal">--save</span></span><br></pre></td></tr></table></figure>
<figure class="highlight ps"><table><tr><td class="code"><pre><span class="line">npm install hexo<span class="literal">-generator-index-plus</span> <span class="literal">--save</span></span><br></pre></td></tr></table></figure>
<p>然后将 <code>_config.yml</code> 中的 <code>index-generator</code> 字段删除，添加如下配置：</p>
<figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="attr">index_generator_plus:</span></span><br><span class="line">  <span class="attr">path:</span> <span class="string">&#x27;&#x27;</span></span><br><span class="line">  <span class="attr">per_page:</span> <span class="number">10</span></span><br></pre></td></tr></table></figure>
<p>无需其他改动，只要在 <code>front-matter</code> 中加入 <code>top: #你的值</code> 就可以排序。<code>top</code> 越大越靠前。</p>
<h2 id="使用"><a href="#使用" class="headerlink" title="使用"></a>使用</h2><p>新建文章：</p>
<figure class="highlight ps"><table><tr><td class="code"><pre><span class="line">hexo new <span class="string">&quot;postname&quot;</span></span><br></pre></td></tr></table></figure>
<p>新建的文章在 <code>source/_post</code> 文件夹里，可以编辑 <code>source/_post/postname.md</code>。</p>
<p>新建页面：</p>
<figure class="highlight ps"><table><tr><td class="code"><pre><span class="line">hexo new page <span class="string">&quot;pagename&quot;</span></span><br></pre></td></tr></table></figure>
<p>新建的页面在 <code>source/pagename</code> 文件夹里，可以编辑 <code>source/pagename/index.md</code>。</p>
]]></content>
      <categories>
        <category>教程</category>
        <category>blog</category>
      </categories>
      <tags>
        <tag>教程</tag>
        <tag>blog</tag>
        <tag>hexo</tag>
      </tags>
  </entry>
  <entry>
    <title>[CF1499D] The Number of Pairs 题解</title>
    <url>/CF1499D/</url>
    <content><![CDATA[<h1 id="CF1499D-The-Number-of-Pairs-题解"><a href="#CF1499D-The-Number-of-Pairs-题解" class="headerlink" title="[CF1499D] The Number of Pairs 题解"></a>[CF1499D] The Number of Pairs 题解</h1><h2 id="Solution"><a href="#Solution" class="headerlink" title="Solution"></a>Solution</h2><p>$c \times \operatorname{lcm}(a, b) - d \times \gcd(a, b) = x$</p>
<p>题目中给的式子既有 $\gcd$ 又有 $\operatorname{lcm}$，我们可以令 $A = \dfrac{a}{\gcd(a, b)}$，$B = \dfrac{b}{\gcd(a, b)}$。</p>
<p>此时 $AB = \dfrac{ab}{\gcd(a, b)^2} = \dfrac{\operatorname{lcm}(a, b) \times \gcd(a, b)}{\gcd(a, b)^2} = \dfrac{\operatorname{lcm}(a, b)}{\gcd(a, b)}$，则 $\operatorname{lcm}(a, b) = AB \times \gcd(a, b)$。</p>
<p>$c \times AB \times \gcd(a, b) - d \times \gcd(a, b) = x$</p>
<p>提取公因式 $\gcd(a, b)$：</p>
<p>$\gcd(a, b) \times (c \times AB - d) = x$</p>
<p>$c \times AB - d$ 是 $x$ 的因数。可以枚举 $x$ 的因数 $f$，则 $AB = \dfrac{f + d}{c}$。</p>
<p>那么 $c$ 是 $f + d$ 的因数。枚举 $c$，得到 $\dfrac{f + d}{c}$。由于 $A$ 和 $B$ 一定是互质的，不能有相同的质因子，所以对于同一种质因子，只有给 $A$ 和给 $B$ 两种情况，并不存在分多少的说法。对答案的贡献就是 $2$ 的 $\dfrac{f + d}{c}$ 因数种数次幂。</p>
<p>一开始，需要把所有数的因数种数预处理出来。</p>
<h2 id="Code"><a href="#Code" class="headerlink" title="Code"></a>Code</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> int long long</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="type">bool</span> is[<span class="number">20000005</span>];</span><br><span class="line"><span class="type">int</span> idx, cnt[<span class="number">20000005</span>], p[<span class="number">4444444</span>];</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">init</span><span class="params">()</span> </span>&#123;</span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">2</span>; i &lt;= <span class="number">20000000</span>; i++) &#123;</span><br><span class="line">		<span class="keyword">if</span> (cnt[i] == <span class="number">0</span>) &#123;</span><br><span class="line">			p[++idx] = i;</span><br><span class="line">			cnt[i] = <span class="number">1</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> j = <span class="number">1</span>; j &lt;= idx &amp;&amp; i * p[j] &lt;= <span class="number">20000000</span>; j++) &#123;</span><br><span class="line">			cnt[i * p[j]] = cnt[i] + <span class="number">1</span>;</span><br><span class="line">			is[i % p[j]] = <span class="number">1</span>;</span><br><span class="line">			<span class="keyword">if</span> (i % p[j] == <span class="number">0</span>) &#123;</span><br><span class="line">				cnt[i * p[j]] = cnt[i];</span><br><span class="line">				<span class="keyword">break</span>;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">sol</span><span class="params">()</span> </span>&#123;</span><br><span class="line">	<span class="type">int</span> c, d, x;</span><br><span class="line">	cin &gt;&gt; c &gt;&gt; d &gt;&gt; x;</span><br><span class="line">	<span class="type">int</span> ans = <span class="number">0</span>;</span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">1</span>; i &lt;= x / i; i++) &#123;</span><br><span class="line">		<span class="keyword">if</span> (x % i) <span class="keyword">continue</span>;</span><br><span class="line">		<span class="keyword">if</span> ((i + d) % c == <span class="number">0</span>) &#123;</span><br><span class="line">			<span class="type">int</span> AB = (i + d) / c;</span><br><span class="line">			ans += (<span class="number">1</span> &lt;&lt; cnt[AB]);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">if</span> (i != x / i) &#123;</span><br><span class="line">			<span class="keyword">if</span> ((x / i + d) % c == <span class="number">0</span>) &#123;</span><br><span class="line">				<span class="type">int</span> AB = (x / i + d) / c;</span><br><span class="line">				ans += (<span class="number">1</span> &lt;&lt; cnt[AB]);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	cout &lt;&lt; ans &lt;&lt; <span class="string">&quot;\n&quot;</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">signed</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">	ios::<span class="built_in">sync_with_stdio</span>(<span class="number">0</span>);</span><br><span class="line">	cin.<span class="built_in">tie</span>(<span class="number">0</span>); cout.<span class="built_in">tie</span>(<span class="number">0</span>);</span><br><span class="line">	<span class="built_in">init</span>();</span><br><span class="line">	<span class="type">int</span> T;</span><br><span class="line">	cin &gt;&gt; T;</span><br><span class="line">	<span class="keyword">while</span> (T--) <span class="built_in">sol</span>();</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>题解</category>
      </categories>
      <tags>
        <tag>题解</tag>
        <tag>CodeForces</tag>
      </tags>
  </entry>
  <entry>
    <title>[CF17B] Hierarchy 题解</title>
    <url>/CF17B/</url>
    <content><![CDATA[<h1 id="CF17B-Hierarchy-题解"><a href="#CF17B-Hierarchy-题解" class="headerlink" title="[CF17B] Hierarchy 题解"></a>[CF17B] Hierarchy 题解</h1><h2 id="Solution"><a href="#Solution" class="headerlink" title="Solution"></a>Solution</h2><p>每个人都有一个上司，除了最顶尖的人没有上司，那么就只有 $n-1$ 个人有上司。</p>
<p>我们把两个人之间的上司关系转换成图论中的有向边，而根据上面得出只会有 $n-1$ 条有向边，这不就是树吗？也就是我们要求这张图的最小生成树。（用的是<code>Kurscal</code>）</p>
<p>先读入每个人的权值和图中的每一条有向边。</p>
<p>再把每一条有向边<strong>按照权值从小到大排序</strong>（因为花的代价越少越好）。</p>
<p>然后循环每一条边，如果 $b$ <strong>没有上司</strong>而且 $a$ <strong>的权值比</strong> $b$ <strong>大</strong>，那么就花费 $c$ 任命 $a$ 为 $b$ 的上司，而且要记录一个变量，表示<strong>利用的边</strong>的个数，每次任命上司的时候这个变量就 $+1$。</p>
<p>如果利用边的个数已经是 $n-1$，那么就跳出。</p>
<p>如果所有边都遍历了一遍，但是利用边的个数却没有达到 $n-1$，那么就表示无解，输出<code>-1</code>。</p>
]]></content>
      <categories>
        <category>题解</category>
      </categories>
      <tags>
        <tag>题解</tag>
        <tag>CodeForces</tag>
      </tags>
  </entry>
  <entry>
    <title>[P3861] 拆分 题解</title>
    <url>/P3861/</url>
    <content><![CDATA[<h1 id="P3861-拆分-题解"><a href="#P3861-拆分-题解" class="headerlink" title="[P3861] 拆分 题解"></a>[P3861] 拆分 题解</h1><h2 id="Solution"><a href="#Solution" class="headerlink" title="Solution"></a>Solution</h2><p>我们可以先求出 $n$ 的所有因数： $x<em>1,x_2,\dots,x</em>{d(n)}$ 。（ $d(n)$ 表示 $n$ 的因数个数）</p>
<p>然后考虑 dp ，设 $dp_{i,j}$ 为把 $x_i$ 分解成若干个 $\leq x_j$ 的数的乘积的方案数。</p>
<p>记录 $pos<em>i$ 为 $i$ 在 $x$ 中的位置，我们可以在 $i\leq sqrt(n)$ 时，记录 $pos</em>{1,i}=pos<em>i$ ，在 $i&gt;sqrt(n)$ 时，记录 $pos</em>{2,\frac{n}{i}}=pos_i$ ，就可以把复杂度缩小到 $O(\sqrt{n})$ 。</p>
<p>然后考虑 $dp<em>{i,j}$ 的求法。首先是初始化，当 $j=1$ 时，可以发现 $i=1$ 即 $x_i=1$ 时 $dp</em>{i,j}=1$ ，否则 $dp<em>{i,j}=0$ 。因为只有 $1$ 可以表示为 $1\times1$ 。加下来考虑传递。首先考虑 $x_i$ 的分解中不包括 $x_j$ 的情况，那么就是把 $x_i$ 分解为若干个小于 $x_j$ 的数的乘积，也就是 $dp</em>{i,j-1}$ 。然后考虑 $x<em>i$ 的分解中包括 $x_j$ 的情况，这种情况首先要求 $x_j\mid x_i$ ，那么接下来就要将 $\dfrac{x_i}{x_j}$ 分解为若干个小于 $x_j$ 的数的乘积，也就是 $dp</em>{\small pos<em>{\frac{x_i}{x_j}},j-1}$ 。将两者加起来即可。最后的答案就是 $dp</em>{d(n),d(n)}-1(x_{d_n}=n)$。</p>
<h2 id="Code"><a href="#Code" class="headerlink" title="Code"></a>Code</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cmath&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;algorithm&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> int long long</span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="type">const</span> <span class="type">int</span> MOD = <span class="number">998244353</span>;</span><br><span class="line"><span class="type">const</span> <span class="type">int</span> N = <span class="number">1000005</span>;</span><br><span class="line"><span class="type">int</span> T, n, sqrtn, tot, pos, x[N], pos1[N], pos2[N], f[<span class="number">6721</span>][<span class="number">6721</span>];</span><br><span class="line"><span class="function"><span class="type">signed</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">	<span class="keyword">for</span> (cin &gt;&gt; T; T--;) &#123;</span><br><span class="line">		cin &gt;&gt; n;</span><br><span class="line">		tot = <span class="number">0</span>, sqrtn = (<span class="type">int</span>)<span class="built_in">sqrt</span>(n);</span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">1</span>; i &lt;= n / i; i++) &#123;</span><br><span class="line">			<span class="keyword">if</span> (n % i == <span class="number">0</span>) &#123;</span><br><span class="line">				x[++tot] = i;</span><br><span class="line">				<span class="keyword">if</span> (i * i != n) x[++tot] = n / i;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="built_in">sort</span>(x + <span class="number">1</span>, x + tot + <span class="number">1</span>);</span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">1</span>; <span class="number">2</span> * i &lt;= tot + <span class="number">1</span>; i++) &#123;</span><br><span class="line">			pos1[x[i]] = i;</span><br><span class="line">			pos2[x[i]] = tot - i + <span class="number">1</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">1</span>; i &lt;= tot; i++) &#123;</span><br><span class="line">			<span class="keyword">if</span> (i == <span class="number">1</span>) f[i][<span class="number">1</span>] = <span class="number">1</span>;</span><br><span class="line">			<span class="keyword">else</span> f[i][<span class="number">1</span>] = <span class="number">0</span>;</span><br><span class="line">			<span class="keyword">for</span> (<span class="type">int</span> j = <span class="number">2</span>; j &lt;= tot; j++) &#123;</span><br><span class="line">				f[i][j] = f[i][j - <span class="number">1</span>];</span><br><span class="line">				<span class="keyword">if</span> (j &gt; i) <span class="keyword">continue</span>;</span><br><span class="line">				<span class="keyword">if</span> (x[i] % x[j] == <span class="number">0</span>) &#123;</span><br><span class="line">					<span class="type">int</span> tmp = x[i] / x[j];</span><br><span class="line">					pos = (tmp &lt;= sqrtn ? pos1[tmp] : pos2[n / tmp]);</span><br><span class="line">					f[i][j] = (f[i][j] + f[pos][j - <span class="number">1</span>]) % MOD;</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		cout &lt;&lt; f[tot][tot] - <span class="number">1</span> &lt;&lt; <span class="string">&quot;\n&quot;</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>题解</category>
      </categories>
      <tags>
        <tag>题解</tag>
        <tag>洛谷</tag>
      </tags>
  </entry>
  <entry>
    <title>[CF1458A] Row GCD 题解</title>
    <url>/CF1458A/</url>
    <content><![CDATA[<h1 id="CF1458A-Row-GCD-题解"><a href="#CF1458A-Row-GCD-题解" class="headerlink" title="[CF1458A] Row GCD 题解"></a>[CF1458A] Row GCD 题解</h1><h2 id="Solution"><a href="#Solution" class="headerlink" title="Solution"></a>Solution</h2><p>众所周知：</p>
<p>$\gcd(a,b)=\gcd(a,b-a)$</p>
<hr>
<p>证明：</p>
<p>设 $\gcd(a,b)=g$ 。则 $a\bmod g=0$；且 $b\bmod g=0$。可以得出 $(b-a)\bmod g=0$。所以 $(a,b-a)$ 和 $(b,b-a)$ 都有公因数 $g$。</p>
<p>可是有可能 $(a,b-a)$ 和 $(b,b-a)$ 的最大公因数比 $g$ 大。所以可以假设 $gcd(a,b-a)=c’&gt;c$。</p>
<p>那么 $a\bmod c’=0$，$(b-a)\bmod c’=0$，且 $(b-a)\bmod c’=b\bmod c’-a\bmod c’$，可以得出 $b\bmod c’=0$。</p>
<p>那么 $\gcd(a,b)=c’&gt;c$，与原来矛盾。$(b,b-a)$ 同理。</p>
<p>故得证。</p>
<hr>
<p>知道这个就简单了，题目让我们求 $\gcd(a_1+b_i,a_2+b_i,a_3+b_i,\dots,a_n+b_i)$，我们可以把 $2\sim n$ 项都减去第 $1$ 项，就变成了 $\gcd(a_1+b_i,a_2-a_1,a_3-a_1,\dots,a_n-a_1)$。可以发现第 $2\sim n$ 项都是不变的，可以预处理出 $2\sim n$ 项的最大公因数，然后枚举 $b_i$ 输出即可。</p>
<h2 id="Code"><a href="#Code" class="headerlink" title="Code"></a>Code</h2><p>AC Code:</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;algorithm&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> int long long</span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="type">int</span> n, m, a[<span class="number">200005</span>], b[<span class="number">200005</span>];</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">gcd</span><span class="params">(<span class="type">int</span> x, <span class="type">int</span> y)</span> </span>&#123;</span><br><span class="line">	<span class="keyword">return</span> y ? <span class="built_in">gcd</span>(y, x % y) : x;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">signed</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">	cin &gt;&gt; n &gt;&gt; m;</span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">1</span>; i &lt;= n; i++) cin &gt;&gt; a[i];</span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">1</span>; i &lt;= m; i++) cin &gt;&gt; b[i];</span><br><span class="line">	<span class="keyword">if</span> (n == <span class="number">1</span>) &#123;</span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">1</span>; i &lt;= m; i++) &#123;</span><br><span class="line">			cout &lt;&lt; a[<span class="number">1</span>] + b[i] &lt;&lt; <span class="string">&quot; &quot;</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="built_in">sort</span>(a + <span class="number">1</span>, a + n + <span class="number">1</span>);</span><br><span class="line">	<span class="type">int</span> c = a[<span class="number">2</span>] - a[<span class="number">1</span>];</span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">2</span>; i &lt;= n; i++) c = <span class="built_in">gcd</span>(c, a[i] - a[<span class="number">1</span>]);</span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">1</span>; i &lt;= m; i++) cout &lt;&lt; <span class="built_in">gcd</span>(a[<span class="number">1</span>] + b[i], c) &lt;&lt; <span class="string">&quot; &quot;</span>;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>题解</category>
      </categories>
      <tags>
        <tag>题解</tag>
        <tag>CodeForces</tag>
      </tags>
  </entry>
  <entry>
    <title>[CF1760D] Challenging Valleys 题解</title>
    <url>/CF1760D/</url>
    <content><![CDATA[<h1 id="CF1760D-Challenging-Valleys-题解"><a href="#CF1760D-Challenging-Valleys-题解" class="headerlink" title="[CF1760D] Challenging Valleys 题解"></a>[CF1760D] Challenging Valleys 题解</h1><h2 id="Solution"><a href="#Solution" class="headerlink" title="Solution"></a>Solution</h2><p>如果一个序列中<strong>恰好有一个</strong>满足以下条件的子序列，输出 <code>YES</code>，否则输出 <code>NO</code>。</p>
<p>条件：</p>
<ul>
<li>$0\leq l\leq r\leq n-1$</li>
<li>$a<em>l=a</em>{l+1}=a_{l+2}=\dots=a_r$</li>
<li>$l=0$ 或 $a_{l-1}&gt;a_l$</li>
<li>$r=n-1$ 或 $a<em>r&gt;a</em>{r+1}$</li>
</ul>
<p>枚举左端点，判断是否符合第三条条件。然后边判断第二条条件，边枚举右端点。当第二条条件不满足时，再判断第四条条件。如果所有都符合，那么答案个数加一。最后如果答案的个数是 $1$，输出 <code>YES</code>，否则输出 <code>NO</code>。</p>
<h2 id="Code"><a href="#Code" class="headerlink" title="Code"></a>Code</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="type">int</span> T, n, a[<span class="number">200005</span>], ans;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">	cin &gt;&gt; T;</span><br><span class="line">	<span class="keyword">while</span> (T--) &#123;</span><br><span class="line">		cin &gt;&gt; n;</span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; n; i++) cin &gt;&gt; a[i];</span><br><span class="line">		ans = <span class="number">0</span>;</span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> l = <span class="number">0</span>; l &lt; n; l++) &#123;</span><br><span class="line">			<span class="keyword">if</span> (l &gt; <span class="number">0</span> &amp;&amp; a[l - <span class="number">1</span>] &lt;= a[l]) <span class="keyword">continue</span>;</span><br><span class="line">			<span class="type">int</span> r = l;</span><br><span class="line">			<span class="keyword">while</span> (r + <span class="number">1</span> &lt; n &amp;&amp; a[l] == a[r + <span class="number">1</span>]) r++;</span><br><span class="line">			<span class="keyword">if</span> (r == n - <span class="number">1</span> || a[r + <span class="number">1</span>] &gt; a[r]) &#123;</span><br><span class="line">				ans++;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">if</span> (ans == <span class="number">1</span>) &#123;</span><br><span class="line">			cout &lt;&lt; <span class="string">&quot;YES\n&quot;</span>;</span><br><span class="line">		&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">			cout &lt;&lt; <span class="string">&quot;NO\n&quot;</span>;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>题解</category>
      </categories>
      <tags>
        <tag>题解</tag>
        <tag>CodeForces</tag>
      </tags>
  </entry>
  <entry>
    <title>[ARC148B] dp 题解</title>
    <url>/ARC148B/</url>
    <content><![CDATA[<h1 id="ARC148B-dp-题解"><a href="#ARC148B-dp-题解" class="headerlink" title="[ARC148B] dp 题解"></a>[ARC148B] dp 题解</h1><h2 id="Solution"><a href="#Solution" class="headerlink" title="Solution"></a>Solution</h2><p>题目要求字典序最小，而且只有 <code>d</code> 和 <code>p</code> 两种字母。所以我们要让在前面的 <code>p</code> 变成 <code>d</code>，可以先找到第一个 <code>p</code> 作为左端点，想让它变成 <code>d</code>，就要找到后面的一个 <code>p</code> 作为右端点，然后暴力枚举就可以了。时间复杂度 $O(n^2)$。</p>
<h2 id="Code"><a href="#Code" class="headerlink" title="Code"></a>Code</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="type">int</span> n, l, r;</span><br><span class="line">string s, t, ans;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">	cin &gt;&gt; n &gt;&gt; s;</span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">		<span class="keyword">if</span> (s[i] == <span class="string">&#x27;p&#x27;</span>) &#123;     <span class="comment">//找到第一个p作为左端点</span></span><br><span class="line">			l = i;</span><br><span class="line">			<span class="keyword">break</span>;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	ans = s;     <span class="comment">//记录答案</span></span><br><span class="line">	<span class="keyword">for</span> (r = l; r &lt; n; r++) &#123;     <span class="comment">//枚举右端点</span></span><br><span class="line">		<span class="keyword">if</span> (s[r] == <span class="string">&#x27;d&#x27;</span>) <span class="keyword">continue</span>;</span><br><span class="line">		t = s;     <span class="comment">//计算重新赋值后的结果</span></span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> i = l; i &lt;= r; i++) &#123;</span><br><span class="line">			<span class="keyword">if</span> (s[i] == s[r + l - i]) &#123;</span><br><span class="line">				t[i] = (t[i] == <span class="string">&#x27;d&#x27;</span> ? <span class="string">&#x27;p&#x27;</span> : <span class="string">&#x27;d&#x27;</span>);     <span class="comment">//对字符取反</span></span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		ans = <span class="built_in">min</span>(ans, t);     <span class="comment">//更新答案</span></span><br><span class="line">	&#125;</span><br><span class="line">	cout &lt;&lt; ans;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>题解</category>
      </categories>
      <tags>
        <tag>题解</tag>
        <tag>AtCoder</tag>
      </tags>
  </entry>
  <entry>
    <title>[CF1814B] Vika and the Bridge 题解</title>
    <url>/CF1814B/</url>
    <content><![CDATA[<h1 id="CF1814B-Vika-and-the-Bridge-题解"><a href="#CF1814B-Vika-and-the-Bridge-题解" class="headerlink" title="[CF1814B] Vika and the Bridge 题解"></a>[CF1814B] Vika and the Bridge 题解</h1><h2 id="Solution"><a href="#Solution" class="headerlink" title="Solution"></a>Solution</h2><p>题目要求从 $(0,0)$ 走到 $(A,B)$ ，而且可以改变步长。我们先要确定一个步长，因为如果 $t_1$ 的步长走几步然后再 $t_2$ 的步长走几步是没有意义的，可以直接用较长的步长替代。</p>
<p>假如步长是 $m$，我们先考虑 $A$ 和 $B$ 都是 $m$ 的倍数的情况，这样所需的步数就是 $\dfrac{A}{m}+\dfrac{B}{m}$，再加上从 $1$ 的步长加到 $m$ 的步长所需的步数 $m-1$，最后加起来就是 $\dfrac{A}{m}+\dfrac{B}{m}+(m-1)$ 步。</p>
<p>然后再考虑 $A$ 或 $B$ 不是 $m$ 的倍数的情况，我们先在 $x$ 坐标走 $\left\lfloor \dfrac{A}{m} \right\rfloor$ 步，然后在 $y$ 坐标走 $\left\lfloor \dfrac{B}{m} \right\rfloor$ 步，我们分别会在 $x$ 和 $y$ 坐标上差 $A\bmod m$ 和 $B\bmod m$ 的距离，那么这段怎么办呢？首先这两段距离肯定小于 $m$，所以我们可以在增加步长时顺便把这段路走掉。最后一共是 $\left\lfloor \dfrac{A}{m} \right\rfloor+1+\left\lfloor \dfrac{B}{m} \right\rfloor+1+(m-1)$。</p>
<p>最后我们把两种情况结合起来，可以得到公式 $\left\lceil \dfrac{A}{m} \right\rceil+\left\lceil \dfrac{B}{m} \right\rceil+(m-1)$。</p>
<p>题目中 $A,B\le10^9$，所以一一便利肯定不行，我们看公式取最小值时 $m$ 是在 $\sqrt{A}$ 左右，所以我们枚举到 $10^5$ 即可。</p>
<h2 id="Code"><a href="#Code" class="headerlink" title="Code"></a>Code</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> int long long</span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="type">int</span> T, a, b, ans;</span><br><span class="line"><span class="function"><span class="type">signed</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">	ios::<span class="built_in">sync_with_stdio</span>(<span class="number">0</span>);</span><br><span class="line">	cin.<span class="built_in">tie</span>(<span class="number">0</span>); cout.<span class="built_in">tie</span>(<span class="number">0</span>);</span><br><span class="line">	cin &gt;&gt; T;</span><br><span class="line">	<span class="keyword">while</span> (T--) &#123;</span><br><span class="line">		cin &gt;&gt; a &gt;&gt; b;</span><br><span class="line">		ans = a + b;           <span class="comment">//初始化 ans 最大值 </span></span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">1</span>; i &lt;= <span class="number">100000</span>; i++) &#123;           <span class="comment">//枚举到 10^5 即可 </span></span><br><span class="line">			ans = <span class="built_in">min</span>(ans, (a + i - <span class="number">1</span>) / i + (b + i - <span class="number">1</span>) / i + (i - <span class="number">1</span>));   <span class="comment">//带入公式 </span></span><br><span class="line">		&#125;</span><br><span class="line">		cout &lt;&lt; ans &lt;&lt; <span class="string">&quot;\n&quot;</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>题解</category>
      </categories>
      <tags>
        <tag>题解</tag>
        <tag>CodeForces</tag>
      </tags>
  </entry>
  <entry>
    <title>[CF1808C] Unlucky Numbers 题解</title>
    <url>/CF1808C/</url>
    <content><![CDATA[<h1 id="CF1808C-Unlucky-Numbers-题解"><a href="#CF1808C-Unlucky-Numbers-题解" class="headerlink" title="[CF1808C] Unlucky Numbers 题解"></a>[CF1808C] Unlucky Numbers 题解</h1><h2 id="Solution"><a href="#Solution" class="headerlink" title="Solution"></a>Solution</h2><p>幸运值是数字中的最大值减去最小值，我们可以设计一个 dfs。</p>
<p>首先，把 $l$ 和 $r$ 处理成两个数组 $a$ 和 $b$，表示每一位的值。</p>
<p>dfs 中设置一个状态 $u$ 表示当前的位数。状态 $num$ 表示数字的 $1 \sim u-1$ 位。然后有两个状态 $Max$ 和 $Min$，表示 $1 \sim u-1$ 位的最大值和最小值。每次枚举 $0 \sim 9$ 表示第 $u$ 位。</p>
<p>但是这样枚举时有可能枚举出界，比如 $l=1,r=5$ 时，枚举第一位有可能枚举到 $0$，还有可能枚举到 $6$。因此我们要再设置两个状态 $isMax$ 和 $isMin$，分别表示当前是否为上界和下界。如果 $isMin$ 为 True，表示是下界，此时枚举第 $u$ 位时只能从 $a_u$ 开始枚举；如果 $isMax$ 为 True，表示是上界，此时枚举第 $u$ 位时只能枚举到 $b_u$。</p>
<p>我有个同学 <del>（都说了同学，怎么会是我）</del>，就这样结果一看记录 <code>Wrong answer on test 2</code>。</p>
<p>那这究竟是道德的沦丧还是人性的扭曲？都不是，这是应为枚举的数字可能含有前导零，这时 $Max$ 和 $Min$ 也会被更新，所以还要设置一个状态 $f$，表示当前是否可以更新 $Max$ 和 $Min$，当且仅当 $1 \sim u-1$ 位都为 $0$ 时 $f$ 为 False。当 $f$ 为 False 且第 $u$ 位不为 $0$ 要把 $f$ 更新成 True。</p>
<p>当然，这样是会 TLE 的。容易发现，数字到底是多少和 dfs 关系并不大。重要的是 $Max$、$Min$、$isMax$、$isMin$ 和 $u$，这关系到后面的数以及答案，所以我们可以设计一个布尔类型的数组，表示当前状态有没有被访问过。这样操作次数最多为 $18 \times 10 \times 10 \times 2 \times 2=7200$，毫无悬念可以通过，我代码最慢也就跑了 $62$ 毫秒。</p>
<h2 id="Code"><a href="#Code" class="headerlink" title="Code"></a>Code</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="type">const</span> <span class="type">int</span> N = <span class="number">25</span>;</span><br><span class="line">string ans;</span><br><span class="line"><span class="type">int</span> n, minans, a[N], b[N];</span><br><span class="line"><span class="type">bool</span> dp[N][<span class="number">10</span>][<span class="number">10</span>][<span class="number">2</span>][<span class="number">2</span>];</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">dfs</span><span class="params">(<span class="type">int</span> u, <span class="type">int</span> Max, <span class="type">int</span> Min, <span class="type">int</span> is_Max, <span class="type">int</span> is_Min, <span class="type">int</span> f, string num)</span> </span>&#123;</span><br><span class="line"><span class="comment">//	cout &lt;&lt; u &lt;&lt; &quot;  &quot; &lt;&lt; Max &lt;&lt; &quot; &quot; &lt;&lt; Min &lt;&lt; &quot;  &quot; &lt;&lt; is_Max &lt;&lt; &quot; &quot; &lt;&lt; is_Min &lt;&lt; &quot;   &quot; &lt;&lt; num &lt;&lt; &quot;\n&quot;;</span></span><br><span class="line">	<span class="keyword">if</span> (u &gt; n) &#123;</span><br><span class="line">		<span class="keyword">if</span> (Max - Min &lt; minans) &#123;</span><br><span class="line">			minans = Max - Min;</span><br><span class="line">			ans = num;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">if</span> (dp[u][Max][Min][is_Max][is_Min]) <span class="keyword">return</span>;</span><br><span class="line">	dp[u][Max][Min][is_Max][is_Min] = <span class="number">1</span>;</span><br><span class="line">	<span class="type">int</span> l = (is_Min ? a[u] : <span class="number">0</span>);</span><br><span class="line">	<span class="type">int</span> r = (is_Max ? b[u] : <span class="number">9</span>);</span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> v = l; v &lt;= r; v++) &#123;</span><br><span class="line">		<span class="keyword">if</span> (v == <span class="number">0</span> &amp;&amp; f == <span class="number">0</span>) &#123;</span><br><span class="line">			<span class="built_in">dfs</span>(u + <span class="number">1</span>, Max, Min, is_Max &amp; (v == r), is_Min &amp; (v == l), f, num + <span class="built_in">char</span>(v + <span class="number">48</span>));</span><br><span class="line">		&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">			<span class="built_in">dfs</span>(u + <span class="number">1</span>, <span class="built_in">max</span>(Max, v), <span class="built_in">min</span>(Min, v), is_Max &amp; (v == r), is_Min &amp; (v == l), f | (v != <span class="number">48</span>), num + <span class="built_in">char</span>(v + <span class="number">48</span>));</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">	<span class="type">int</span> T;</span><br><span class="line">	cin &gt;&gt; T;</span><br><span class="line">	<span class="keyword">while</span> (T--) &#123;</span><br><span class="line">		string l, r;</span><br><span class="line">		cin &gt;&gt; l &gt;&gt; r;</span><br><span class="line">		<span class="keyword">while</span> (l.<span class="built_in">size</span>() &lt; r.<span class="built_in">size</span>()) l = <span class="string">&quot;0&quot;</span> + l;</span><br><span class="line">		n = l.<span class="built_in">size</span>();</span><br><span class="line">		l = <span class="string">&quot;0&quot;</span> + l, r = <span class="string">&quot;0&quot;</span> + r;</span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">1</span>; i &lt;= n; i++) &#123;</span><br><span class="line">			a[i] = l[i] - <span class="number">48</span>, b[i] = r[i] - <span class="number">48</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="built_in">memset</span>(dp, <span class="number">0</span>, <span class="keyword">sizeof</span> dp);</span><br><span class="line">		minans = <span class="number">1000</span>;</span><br><span class="line">		<span class="built_in">dfs</span>(<span class="number">1</span>, <span class="number">0</span>, <span class="number">9</span>, <span class="number">1</span>, <span class="number">1</span>, <span class="number">0</span>, <span class="string">&quot;&quot;</span>);</span><br><span class="line">		<span class="type">int</span> flag = <span class="number">0</span>;</span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; (<span class="type">int</span>)ans.<span class="built_in">size</span>(); i++) &#123;</span><br><span class="line">			<span class="keyword">if</span> (ans[i] != <span class="number">48</span>) flag = <span class="number">1</span>;</span><br><span class="line">			<span class="keyword">if</span> (flag) cout &lt;&lt; ans[i];</span><br><span class="line">		&#125;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;\n&quot;</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>题解</category>
      </categories>
      <tags>
        <tag>题解</tag>
        <tag>CodeForces</tag>
      </tags>
  </entry>
  <entry>
    <title>test</title>
    <url>/test/</url>
    <content><![CDATA[<h1 id="test"><a href="#test" class="headerlink" title="test"></a>test</h1><p>这是一段文字。</p>
<p><a href="https://ikunhorro.github.io/">link</a></p>
<p>$E = mc ^ {2}$</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>test</category>
      </categories>
      <tags>
        <tag>test</tag>
      </tags>
  </entry>
</search>
